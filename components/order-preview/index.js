import { roleType } from '../../enum/role-type'

Component({

    properties: {
        order: Object,
        role: Number
    },

    data: {
        roleTypeEnum: roleType
    },

    methods: {}
})
