<view class="container">
    <view class="header">
        <i-avatar bind:tap="handleToLogin" src="{{userInfo.avatar}}" text="{{userInfo.nickname}}" shape="circle" fontSize="34"></i-avatar>
    </view>
</view>